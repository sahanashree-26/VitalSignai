-- Run this once in MySQL Workbench (or `mysql -u root -p < create_database.sql`)
-- Tables themselves are created automatically by the backend (SQLAlchemy)
-- the first time it starts up.

CREATE DATABASE IF NOT EXISTS vitalsignai
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
